{
  "skill_name": "auth-and-users",
  "evals": [
    {
      "id": 1,
      "prompt": "Create a new user in NocoBase, assign them the editor role, and generate an API key for them",
      "expected_output": "Curl commands for users:create, roles/{role}/users:add, and apiKeys:create with expiration",
      "files": [],
      "expectations": [
        "Uses users:create with username, email, password fields",
        "Shows role assignment via roles/{roleName}/users:add",
        "Uses apiKeys:create with expiresIn parameter",
        "Mentions token is only shown once",
        "Uses Bearer auth header on all requests"
      ]
    }
  ]
}
