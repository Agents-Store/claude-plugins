[
  {
    "event": "afterToolResult",
    "tools": ["Write", "Edit"],
    "prompt": "IMPORTANT: You just modified a file in the OpenClaw workspace. OpenClaw runs as `node` inside Docker. Host-side edits (as root or another user) break file permissions.\n\nAfter completing your current batch of edits, you MUST fix permissions:\n\n1. Find the docker compose project directory:\n   ls /docker/ | grep openclaw\n\n2. Fix ownership:\n   cd /docker/openclaw-{instance} && docker compose exec -u root openclaw-gateway chown -R node:node /home/node/.openclaw/\n\n3. Run doctor to verify:\n   openclaw-team doctor --fix\n\n`docker compose exec` automatically targets the correct container from the docker-compose project directory. No container name needed.\n\nAsk the user which openclaw instance directory to use if unclear. Batch all edits first, then fix permissions once at the end."
  }
]
